global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: "synergy-switch"
    metrics_path: /metrics
    static_configs:
      - targets: ["switch:8080"]
        labels:
          instance: "synergy-switch"
